# Diretório de configuração / worktrees do Claude Code
.claude/

# Arquivos temporários / scripts de auditoria
*.tmp
*.bak
*.log
recon*.json
recon.py
debug_main.js
probe.html

# Diretório efêmero de saída (entregáveis finais ficam em docs/auditoria-*/)
outputs/

# Backups manuais (versionamento real está no git)
backup/

# Dependências
node_modules/

# Saída de build/extract
tests/dist/

# Artefato do build modular (refactor-modular-v1): regenerável via
# `python src/build/build.py`. Não commitar pra evitar diff gigante a cada
# fase de extração. Distribuição final (Fase 6) vai em release tag/CI.
dist/

# Sistema
.DS_Store
Thumbs.db
